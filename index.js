const age = 24;

const name = "Ram Bharoshe";

const dob = "03/09/2024";

const isAdult = () => {
  return age >= 18;
  console.log(
    "isAdult called with     " + age + " years old and   " + age + " years old");
};
